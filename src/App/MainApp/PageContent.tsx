import SharedPageContent from '@libs/ui-kit/components/PageContent';

const PageContent: React.FC = ({ children }) => (
  <SharedPageContent>{children}</SharedPageContent>
);

export default PageContent;
