import SharedPageContainer from '@libs/ui-kit/components/PageContainer';

const PageContainer: React.FC = ({ children }) => (
  <SharedPageContainer>{children}</SharedPageContainer>
);

export default PageContainer;
